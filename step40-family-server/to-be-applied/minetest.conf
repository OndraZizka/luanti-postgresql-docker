#CONFIG_FILE=./minetest-data/main-config/minetest.conf

server_announce = true
server_name = "Ada's easy Luanti server."
server_description =  "Our simple family server where we gather for some fun. Be nice."
server_address = luanti.zizka.ch
server_url = https://github.com/OndraZizka/luanti-postgresql-docker
default_game = mineclonia

default_privs = interact, shout, home, fast
enable_damage = true

mcl_difficulty = easy
mcl_keepInventory = true
mcl_playersSleepingPercentage = 1
## For the Bones mod, not sure when this works.
bones_mode = keep

## Server admin user name
name = Ondra